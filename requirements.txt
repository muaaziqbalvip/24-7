pyTelegramBotAPI==4.26.0
requests==2.32.3
duckduckgo-search==6.1.7
fpdf2==2.8.2
arabic-reshaper==3.0.0
python-bidi==0.4.2
python-docx==1.1.2
firebase-admin==6.5.0
beautifulsoup4==4.12.3
googlesearch-python==1.2.5
ascii-magic==2.3.0
google-generativeai==0.8.3
pillow==11.1.0
python-dotenv==1.0.1
grpcio==1.64.1
protobuf==4.25.3

# NOTE (V22): Voice replies now use Groq's PlayAI-TTS via REST API
# (NeuralEngine.call_groq_tts), so gTTS/pydub/SpeechRecognition are no
# longer required. If you deploy on a server without common system
# fonts, install `fonts-dejavu-core` (apt) so the trading-signal image
# annotation (TradingSignalEngine.draw_signal_on_chart) has a bold font
# to render the UP/DOWN banner text -- it silently falls back to a
# built-in font if that's missing, but bold DejaVu looks best.
